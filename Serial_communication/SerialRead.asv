function result = SerialRead(COMPort) 
    data = read(COMPort, 20, 'uint8');
    
    try
        ASCIIData = char(data);
    catch
        try
            ASCIIData = hex2dec(data)
    result = ASCIIData;
end